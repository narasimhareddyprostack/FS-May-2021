let AllSizes = [38, 39, 40, 42];

/* let newSizes = AllSizes.map((size) => {
  return size + 3;
}); */

let newSizes = AllSizes.map((size) => size + 3);
console.log(newSizes);
// map() - iterate array, return new array
//forEach() - iterate array object, and return null
