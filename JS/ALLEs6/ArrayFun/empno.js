let emp_No = [101, 102, 103, 104];
/* 
Array - iterable object
Object- not iterable object/iteration is possible
for (let i = 0; i <= emp_No.length - 1; i++) {
  console.log(emp_No[i]);
}

while() 

do{} while

for of - array
for in - object
 */

for (no of emp_No) {
  console.log(no);
}
